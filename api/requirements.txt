# JudgeMatrixSE API — Python dependencies
# Install with: pip install -r requirements.txt

Django>=5.2,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.3,<5.0
numpy>=1.26,<3.0
Pillow>=10.0,<12.0          # for avatar uploads (Phase 0 user profile)
dj-database-url>=2.2,<3.0
gunicorn>=22.0,<24.0
psycopg[binary]>=3.2,<4.0
whitenoise>=6.7,<7.0

# LLM providers
openai>=1.30,<2.0           # set LLM_PROVIDER=openai or deepseek in env
# anthropic>=0.28,<1.0       # set LLM_PROVIDER=anthropic in env

# Dev / test
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
